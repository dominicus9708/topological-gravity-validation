Our galaxy Halo Stellar Kinematics - topological stage 017

Changes from stage 016
- Standard and topological outputs are separated when direct comparison is not physically same-unit.
- The old distance_to_observation plot is split into standard-only and topological-only plots.
- 6D observation panel no longer uses continuous fill_between. It uses shell-wise vertical interval plotting instead.
- Graph style is unified: observation and topological panels both use interval-style vertical rendering.
